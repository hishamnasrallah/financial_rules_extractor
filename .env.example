# ========================================
# aiXplain API Configuration
# ========================================
AIXPLAIN_API_KEY=your_api_key_here

# aiXplain Model IDs (optional - leave empty for auto-discovery)
# The system will automatically discover and use the best available models
# Or specify exact model IDs if you have access to specific models
AIXPLAIN_EMBEDDING_MODEL_ID=
AIXPLAIN_LLM_MODEL_ID=
AIXPLAIN_SEARCH_MODEL_ID=

# ========================================
# RAG (Retrieval-Augmented Generation) Configuration
# ========================================

# Vector Database: ChromaDB (Persistent Storage)
# Data stored in: data/chroma_db/ directory
AIXPLAIN_INDEX_NAME=financial_rules_index
AIXPLAIN_DATASET_NAME=financial_rules_dataset

# Chunking Settings
CHUNK_SIZE=500
CHUNK_OVERLAP=100
RETRIEVAL_TOP_K=5

# Enable/Disable RAG Mode
USE_RAG=true

# ========================================
# Performance Settings
# ========================================

# Quick Mode: Disable LLM for faster processing (30-60 seconds instead of 10-15 minutes)
# Set to 'true' to use only pattern-based extraction without LLM calls
# Set to 'false' to use LLM for better accuracy (slower)
DISABLE_LLM=false

# ========================================
# External Integrations (Optional)
# ========================================

# Slack Integration
SLACK_WEBHOOK_URL=
SLACK_CHANNEL=#financial-rules

# Discord Integration
DISCORD_WEBHOOK_URL=

# Email Integration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
NOTIFICATION_EMAIL=

# Generic Webhook
CUSTOM_WEBHOOK_URL=
WEBHOOK_METHOD=POST

# Enable/Disable All Notifications
ENABLE_NOTIFICATIONS=false

# ========================================
# Application Settings
# ========================================

# Logging
LOG_LEVEL=INFO

# Retry Settings
MAX_RETRIES=3
TIMEOUT_SECONDS=60

# Feature Flags
ENABLE_DYNAMIC_TRACKS=true

# ========================================
# Notes
# ========================================
# 1. Get your aiXplain API key from: https://platform.aixplain.com/
# 2. For model IDs, visit: https://console.aixplain.com/marketplace
# 3. Leave model IDs empty for automatic model discovery
# 4. Set DISABLE_LLM=true for quick testing (pattern-based extraction only)
# 5. Set USE_RAG=true for best results (retrieval-augmented generation)
# 6. Configure integrations only if you want automatic notifications
